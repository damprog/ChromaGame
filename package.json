{
  "name": "chromatron-like",
  "private": true,
  "scripts": {
    "web:dev": "pnpm -C web dev",
    "web:build": "pnpm -C web build",
    "web:start": "pnpm -C web start",
    "web:lint": "pnpm -C web lint"
  }
}
